#include<stdio.h>
int main()
{
int a, b;
printf("enter the first digit\n");
scanf("%d", &a);
printf("enter the second digit\n");
scanf("%d", &b);
printf("the sum the given digits is %d\n", a+b);
printf("the difference the given digits is %d\n", a-b);
printf("the product the given digits is %d\n", a*b);
printf("the quotient the given digits is %d\n", a/b);
    return 0;
}
