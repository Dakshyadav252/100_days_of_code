#include <stdio.h>
int main()
{
    int a, b;
    printf("input the first digit\n");
    scanf("%d", &a);
    printf("input the second digit\n");
    scanf("%d", &b);
    printf("the sum of the given digits is: %d", a + b);
    return 0;
}